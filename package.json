{
  "name": "stevennoack.de",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "tinacms dev -c \"astro dev\"",
    "build": "tinacms build && astro check && astro build"
  },
  "dependencies": {
    "@tinacms/cli": "^1.11.0",
    "astro": "^5.15.1",
    "tinacms": "^2.9.0"
  }
}
