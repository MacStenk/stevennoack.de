---
/**
 * Contact Section Component
 * @description Contact links and info
 */
---

<section id="contact" class="section">
  <h2 class="section-title">&gt; contact</h2>
  <div class="contact-content">
    <p class="contact-intro">
      Let's connect. I'm always open to interesting conversations.
    </p>
    <div class="contact-links">
      <a
        href="https://github.com/MacStenk"
        target="_blank"
        rel="noopener"
        class="contact-link"
      >
        <span class="contact-icon">→</span> GitHub: @MacStenk
      </a>
      <a href="mailto:steven@stevennoack.de" class="contact-link">
        <span class="contact-icon">→</span> Email: steven@stevennoack.de
      </a>
    </div>
  </div>
</section>

<style>
  .section {
    padding: 4rem 0;
    border-top: 1px solid var(--border-color);
  }

  .section-title {
    font-size: clamp(2rem, 5vw, 3rem);
    color: var(--matrix-green);
    margin-bottom: 3rem;
    font-weight: 700;
  }

  .contact-content {
    max-width: 600px;
  }

  .contact-intro {
    font-size: 1.1rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
  }

  .contact-links {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .contact-link {
    font-size: 1.1rem;
    color: var(--matrix-green);
    text-decoration: none;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .contact-link:hover {
    transform: translateX(10px);
  }

  .contact-icon {
    color: var(--matrix-green);
  }

  @media (max-width: 768px) {
    .section {
      padding: 3rem 0;
    }
  }
</style>
